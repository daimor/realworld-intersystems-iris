version: '3.7'

services:
  server:
    build:
      context: .
    init: true
    volumes:
      - db:/home/irisowner/conduit/db
      - ./:/home/irisowner/conduit
    ports:
      - 12000:52773
  web:
    build:
      context: web
      dockerfile: Dockerfile-${web:-vue}
    ports:
      - 80:80
volumes:
  db:
